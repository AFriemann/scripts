#!/bin/sh



function contains_push () {
  for file in $@/*;
  do
    echo $file
    if [[ $file =~ "push" ]]; then
      return 0
    fi
  done

  return 1
}

for repo in $GITHOME/*; 
do
  if contains_push $repo; then
    cd $repo
    ./push
    popd &>/dev/null
  fi
done
